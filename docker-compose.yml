version: "3.7"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    stdin_open: true
    tty: true
    volumes:
      - .:/app
   # depends_on:
   #   - solr
   #   - mysql
    ports:
      - "3000:3000"
   # environment:
   #   SOLR_URL: "http://solr:8983/solr/blacklight-core"
   #   DEVELOPMENT_ENVIRONMENT: "docker"
    entrypoint:
      - /app/bin/start.sh

  #solr:
  #  image: solr:7
  #  volumes:
  #    - $PWD/lib/generators/blacklight/templates/solr/conf:/opt/solr/conf
  #  ports:
  #    - "8983:8983"
  #  entrypoint:
  #    - docker-entrypoint.sh
  #    - solr-precreate
  #    - blacklight-core
  #    - /opt/solr/conf
  #    - "-Xms256m"
  #    - "-Xmx512m"

  #mysql:
  #  image: mysql:5.7
  #  restart: always
  #  environment:
  #    MYSQL_DATABASE: 'HarvardDigitalCollections'
  #    # So you don't have to use root, but you can if you like
  #    MYSQL_USER: 'HarvardDigitalCollections'
      # You can use whatever password you like
  #    MYSQL_PASSWORD: 'HarvardDigitalCollections'
      # Password for root access
  #    MYSQL_ROOT_PASSWORD: 'HarvardDigitalCollections'
  #  ports:
  #    # <Port exposed> : < MySQL Port running inside container>
  #    - '3306:3306'
  #    # Where our data will be persisted
  #  volumes:
  #    - db-data:/var/lib/mysql
# Names our volume
#volumes:
#  db-data:
#    driver: local